# Copy this file to `.env` for local development, or add these
# variables in Vercel (Settings -> Environment Variables) for production.

# Base URL of the PrevPaperApp backend (API gateway)
VITE_API_URL=http://localhost:8080

# Dedicated global-admin service (only needed if hosted separately)
VITE_GLOBAL_ADMIN_URL=http://localhost:8082
